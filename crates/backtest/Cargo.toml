[package]
name = "quantis-backtest"
description = "Event-driven backtesting engine; single source of truth for fill logic"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
quantis-core.workspace = true
quantis-market-data = { path = "../market-data" }
rand = "0.9"
rand_chacha = "0.9"
serde.workspace = true
serde_json = "1"
thiserror.workspace = true

[lints]
workspace = true

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "engine"
harness = false

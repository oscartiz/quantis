[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "quantis-core"
description = "Rust core bindings for the Quantis research layer"
requires-python = ">=3.11"
dynamic = ["version"]

[tool.maturin]
module-name = "quantis_core"
features = ["extension-module"]

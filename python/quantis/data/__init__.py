"""Data access (Phase 2): loaders for recorded event logs and candle history,
plus the holdout wall — the walled-off out-of-sample span whose hash is
committed before research begins and which is evaluated exactly once."""
